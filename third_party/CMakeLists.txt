# libana third-party dependencies placeholder configuration
# Future packages (e.g., micro-testing suites, custom plot wrappers) hook here.

message(STATUS "libana: third_party dependencies configured.")